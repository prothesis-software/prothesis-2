git:
  depth: 1
cache:
  directories:
  - $HOME/wxwidgets
language: cpp
install:
  - sudo apt-get -qq update
  - sudo apt-get install -y gtk2.0 gcc-mingw-w64-x86-64
  - ./build-wxwidgets.sh
  - make get-deps
script:
  - make linux
  - make windows
